# Production — committed, no secrets.
# Inject MONGODB_URI, JWT_SECRET, CLAUDE_API_KEY, ADMIN_SECRET via CI/CD env vars.

NODE_ENV=production
PORT=5000
JWT_EXPIRES_IN=7d
LOG_LEVEL=warn
CORS_ORIGIN=https://reviewboost.in
